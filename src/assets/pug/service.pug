html(lang="en")
  head
    script.
      var calcFontSize = function () {
        var dWidth = window.innerWidth;
        var fontSize;
        if( dWidth > 1024) {
          fontSize = dWidth/1440;
        }
       else if (window.innerHeight > window.innerWidth) {
          fontSize = (dWidth/375) * (window.innerWidth/window.innerHeight < 0.72 ? 1 : 0.8);
        }
        else {
          fontSize = dWidth/667 * (window.innerHeight/window.innerWidth < 0.72 ? 1 : 0.8);
        }
        document.getElementsByTagName('html')[0].style.fontSize = '' + fontSize + 'rem';
     };
     calcFontSize();
     window.onresize = calcFontSize;
    title Service charge
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    link(rel="stylesheet" href="css/style.css")
    link(rel="stylesheet" href="plugins/bootstrap/dist/css/bootstrap.css")
  body
    .container-fluid.flex-parent
      include modules/header

      .row.flex-content
        .step1.col-xl-10.offset-xl-1.col-md-12.offset-md-0
          .row.padding-18
            .step1__desc.col-md-6.col-12.order-md-1.order-2
              .step-desc-wrap
                h1.title1
                  | Your service price is among the defining factors for potential customers
                p.subtitle
                  | We will ask you a few question that will help us personalize your account 
                  | and start sending the jobs your way.

            .step1__image.col-md-6.col-12.order-md-2.order-1
              img.step1-logo(src="img/service-logo.svg", alt="")
              
      include modules/footer1

    script(type="text/javascript" src="plugins/jquery/dist/jquery.min.js")
    script(type="text/javascript" src="plugins/bootstrap/dist/js/bootstrap.min.js")   
    script(type="text/javascript" src="js/script.js")