html(lang="en")
  head
    script.
      var calcFontSize = function () {
        var dWidth = window.innerWidth;
        var fontSize;
        if( dWidth > 1024) {
          fontSize = dWidth/1440;
        }
       else if (window.innerHeight > window.innerWidth) {
          fontSize = (dWidth/375) * (window.innerWidth/window.innerHeight < 0.72 ? 1 : 0.8);
        }
        else {
          fontSize = dWidth/667 * (window.innerHeight/window.innerWidth < 0.72 ? 1 : 0.8);
        }
        document.getElementsByTagName('html')[0].style.fontSize = '' + fontSize + 'rem';
     };
     calcFontSize();
     window.onresize = calcFontSize;
    title Introduction
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    link(rel="stylesheet" href="css/style.css")
    link(rel="stylesheet" href="plugins/bootstrap/dist/css/bootstrap.css")
  body
    .container-fluid.flex-parent
      include modules/header

      .row.margin-fixed.flex-content
        .col-xl-4.col-lg-5.col-md-6.col-12.left-bar
          .left-padding
            .left-bar__cont-width
              h1.title.top-56
                | Why should customers hire you?
              p.subtitle
                | Explain what makes you stand out and why you’ll do a great job.
              .mention
                h3.title3
                  | You can mention
                ul
                  li Professional background;
                  li Education;
                  li What you are passionate about;
                  li Special skills.
        .col-xl-6.col-lg-5.col-md-6.col-12.right-bar.offset-md-0.offset-lg-1
          .right-padding
            .row
              .col-lg-12
                h3.title3.top-56
                  | Introduce yourself
                p.subtitle
                  | Type all you want in the field “Introduction”. You can use example introducton.
                form(action="#")
                  .input-wrap2
                    label.l-name.l-name-textarea Introduction
                    .input-textarea
                      textarea.col-12(name="introduction" rows="10" cols="45" placeholder="Introdius your self" maxlength="500" required)
                      span.calc-characters 0/500 characters
                .wrap-56
                  h3.title3
                    | Example introduction
                  p.subtitle
                    | You can use this as a template for your introduction
                  p.description
                    | I enjoy working with the client and taking the time to listen to each of their 
                    | individual needs. This is not a one fit for all type of business. Each client has 
                    | a specific set of needs and should be addressed and handled in that very same manner.
                    | Be it helping an individual client with a tax issue or assisting a business client 
                    | realize a goal or milestone, I'm not satisfied until I know that my client is happy.
                  .btn-right
                    button.mdc-button NEXT EXAMPLE
                    //- a(href="#").btn-green
                    //-   | next example
                    img.arrow-right-green(src="img/arrow-right-green.svg", alt="")
          
      include modules/footer1

    script(type="text/javascript" src="plugins/jquery/dist/jquery.min.js")
    script(type="text/javascript" src="plugins/bootstrap/dist/js/bootstrap.min.js")   
    script(type="text/javascript" src="js/script.js")